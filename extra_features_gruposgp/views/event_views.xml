<?xml version="1.0" encoding="utf-8"?>
<odoo>

<record model="ir.ui.view" id="extra_features_gruposgp_view_event_registration_form">
    <field name="name">event.registration.form.extra.features</field>
    <field name="model">event.registration</field>
    <field name="inherit_id" ref="event.view_event_registration_form" />
    <field name="arch" type="xml">
        <field name="name" position="after">
            <field name="function" string="Job Title"/>
            <!-- It's obviously not the company name | we stay put until we now what to do  -->
            <field name="company_name" string="Company name"/>
        </field>
    </field>
</record>

</odoo>